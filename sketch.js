var bananaImage
var obstacleImage , obstacleGroup
var background
var score

function preload() {
  
}

function setup() {
  createCanvas(400, 400);
  
  banana = createSprite(400,400);
}

function draw() {
  background(220);
}